prefix = @prefix@
exec_prefix = @exec_prefix@
libdir = @libdir@
includedir = @includedir@

Name : libsincity
Description : SinCity library
Version : @PACKAGE_VERSION@
Requires: 
Requires.private: tinySAK >= @DOUBANGO_MINVER@ tinyNET >= @DOUBANGO_MINVER@ tinySDP >= @DOUBANGO_MINVER@ tinyBFCP >= @DOUBANGO_MINVER@ tinyRTP >= @DOUBANGO_MINVER@ tinyMEDIA >= @DOUBANGO_MINVER@ tinyMSRP >= @DOUBANGO_MINVER@ tinyDAV >= @DOUBANGO_MINVER@
Conflicts:
Cflags : -I${includedir}/sincity
Libs : -L${libdir} -lsincity
Libs.private: @LIBFFMPEG_LIBADD@ @LIBH264_LIBADD@ @LIBVPX_LIBADD@ @LIBOPUS_LIBADD@ @LIBAMR_LIBADD@ @LIBSPEEX_LIBADD@ @LIBSPEEXDSP_LIBADD@ @LIBGSM_LIBADD@ @LIBG729_LIBADD@ @LIBILBC_LIBADD@ @LIBYUV_LIBADD@ @LIBWEBRTC_LIBADD@

